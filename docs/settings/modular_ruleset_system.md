### The Ante-Chamber of Configuration  
**Modular Rule-Set Selection & Customization System**  
*(Pre-Load, Immutable by Design — XODEX v0.00.5.0+)*

Before any tower silhouette touches the grid, before the first wave seed is drawn from the river, the traveler enters the **Ante-Chamber** — a floating hexagonal gallery suspended between the main menu and the game world.

This chamber is the only place where the full syntactic flood of possible rules may still be tamed by deliberate choice. Once “Seal Configuration & Enter Geometry” is pressed, the Projection Operator takes over: the chosen rule set is serialized, checksummed, and projected onto the invariant manifold. All subsequent play is deterministic, reproducible, and eternally faithful to the axioms selected here.

#### The Sacred Process (step by step)

1. **Access the Sanctum**  
   From the main menu → **Options** → **Rule Systems**.  
   The screen dissolves into a slow-rotating archive gallery. Six primary plinths orbit a central zero-point orb (the current projected density δ(L) visualized as a thinning sphere).

2. **Modular Rule-Set Selection**  
   Each plinth represents an independent, formally verified **Rule Module** drawn from the immutable `archive/`:
   - **Thermal Regime** (overheat curves, recovery functions, diffusion models)  
   - **Wave Entropy Dynamics** (spawn functions, aggression, procedural seeds)  
   - **Tower Taxonomy** (allowed classes, synergy matrices, placement constraints)  
   - **Path & Coverage Invariants** (mandatory coverage rules, leak penalties)  
   - **Boss Stress Tests** (invariant-breaking modifiers)  
   - **Victory & Collapse Conditions** (health decay laws, win thresholds, total-collapse triggers)  
   Higher-order expansions (Crimson Projection Mode, Borges Wave Library, Zero-Drift Respec Lock) appear as locked plinths unlockable via archive progression.

   Toggle entire modules on/off with a single tap. Activating additional modules increases recursive layering depth \(L\), visibly thinning the glowing density sphere in real time — a direct manifestation of Burzen’s Law.

3. **Deep Parametric Customization**  
   Select an active module → the right wall unfurls into precise controls:
   - Sliders with live mathematical preview (e.g., “Thermal Overheat Threshold = 87.4 → projected recovery half-life 14.2s”)  
   - Preset curves pulled from `archive/tower_definitions/` and `archive/simulation_traces/`  
   - Direct expression input field (validated against Haskell proofs in real time; invalid expressions are softly rejected with a gentle “noise detected” shimmer)  
   - Synergy matrix editor (drag-and-drop tower interactions with heat-map visualization)

   Every change triggers an instant micro-projection: the system recomputes δ(L), compatibility matrix, and projected difficulty. Warnings appear only when the combination risks critical thinning (“This rule set approaches measure zero — proceed with reverence”).

4. **Live Projection Preview**  
   At the bottom of the chamber floats a shimmering `#XDX1` token fragment that updates live.  
   A miniature deterministic replay (10-second ghost simulation) plays in a small viewport, showing exactly how the chosen rules will feel once loaded.  
   The central zero-point orb pulses with the current valid-density percentage — a direct numerical reminder that syntax is abundant, but surviving geometry is rare.

5. **Final Sealing & Projection**  
   Press **Seal Configuration & Enter Geometry**.  
   The system:
   - Serializes the entire rule set into a compact payload  
   - Applies LZ compression + checksum8  
   - Runs the full Projection Operator (idempotent + faithful)  
   - If the combination lies on the invariant manifold, the token is finalized and the game world materializes around the chosen axioms.  
   If any drift is detected, the chamber gently returns the traveler to customization with a single highlighted conflict.

   The resulting game session is now eternally reproducible by anyone who receives the final `#XDX1` share link.

---

This system is the living embodiment of the Projection Operator Theorem: the player becomes co-architect of the Crimson Hexagon for one session, then steps aside and lets the geometry enforce truth without mercy.
